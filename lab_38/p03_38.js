// 1. Define an array of at least 3 user objects
// 2. Each object should have name and age properties
let users = [
    { name: "Alice", age: 25 },
    { name: "Bob", age: 30 },
    { name: "Charlie", age: 22 }
];

// 3. Use console.table() to display the data in a structured format
console.table(users);